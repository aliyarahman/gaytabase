Django/Python app that collects, manages, matches, triangulates, consolidates all of our existing research for the nondiscrimination campaign


Note, this is deployed on Bluehost shared hosting with Fast CGI, which does all kinds of unusual things around serving static files, setup, etc. To handle the admin static files, created a static folder in the top level of the project (not app) folder, and set ADMIN_MEDIA_PREFIX = '/admin/static/'
